export enum Command {
    SetBasis = "setbasis",
    ViewBasis = "viewbasis",
    AddTarget = "addtarget",
    RemoveTarget = "removetarget",
    ViewTargets = "viewtargets",
    Help = "help",
    Unlink = "unlink",
}
